{
  "name": "peasy-proto",
  "version": "1.0.0",
  "description": "Peasy App Prototype",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "babel-build": "babel src -d build",
    "msx-watch": "msx --watch src/ build/",
    "dev": "concurrently --kill-others \"msx --watch src/ build/\" \"babel src -d build\"",
    "start": "npm run dev"
  },
  "keywords": [
    "mithril"
  ],
  "author": "Alexandra Haynes",
  "license": "MIT",
  "dependencies": {
    "mithril": "^0.2.5",
    "msx": "^0.4.1"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-latest": "^6.16.0",
    "concurrently": "^3.1.0"
  }
}
